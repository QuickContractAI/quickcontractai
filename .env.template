# Copy this file to `.env` and fill in the values before running the app.
# setup.sh creates the .env automatically from this template on first run.

# --- Google Generative AI (Gemini 2.5 Pro for draft generation) ---
GOOGLE_API_KEY=

# --- Snowflake (Cortex Search Service backing the RAG layer) ---
# Defaults below match the training-account setup the project was built on;
# replace them with your own account/role/warehouse if you re-provision.
SNOWFLAKE_ACCOUNT=SFEDU02-PDB57018
SNOWFLAKE_USER=CAT
SNOWFLAKE_PASSWORD=
SNOWFLAKE_ROLE=TRAINING_ROLE
SNOWFLAKE_WAREHOUSE=DAMG7374
SNOWFLAKE_DATABASE=LAWS_CONTRACTS
SNOWFLAKE_SCHEMA=TEXT
